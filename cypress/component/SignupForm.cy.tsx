import SignupForm from "../../client/src/components/SignupForm";
import { mount } from "cypress/react";

describe("<SignupForm />", () => {
  it("TODO", () => {
    mount(<SignupForm handleModalClose={() => console.log("Modal closed")} />);
    console.log("TODO");
  });
});
